export enum STATUS {
    "ACTIVE" = "bg-emerald-600",
    "DISABLED" = "bg-rose-500",
    "EXPIRING" = "bg-amber-400",
}

export enum CONTEXT_METHOD {
    "NONE" = "none",
    "SLIDING_WINDOW" = "100",
}